
userDeletePost('/user-delete-post',url, async(req,res)=>
{
try(!req.files)
  {
    status: false,
    message: 'No post was deleted' 
  }

  
  
  const jwt = require("jsonwebtoken");
const signingCertificate = '-----BEGIN CERTIFICATION-----'
---END CERTIFICATE---
car claims = jwt.verify(token, signingCertificate)
if(claims.exp && claims.exp <=new Date().getTime()/1000)
throw "expired";
var user = claims.sub
}


